#include <iostream>
#include <fstream>
using namespace std; 

int main () 
{
    //create and open a text
    ofstream MyFile("exampleFile.txt");

    // close the file

    void open (const char example, ios::openmode mode);
    

    return 0;
}